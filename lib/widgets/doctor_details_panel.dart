import 'package:flutter/material.dart';

class DoctorDetailsPanel extends StatelessWidget {
  const DoctorDetailsPanel({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: child,
    );
  }
}